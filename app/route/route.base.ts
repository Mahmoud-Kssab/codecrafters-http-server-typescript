// import { RouteCompiler } from "./route-compiler";

// export class RouteBase {
//   protected  matchToKeys(array $values)
//   {
//       $parameters = [];

//       // Get parameter names from the compiled route
//       $keys = $this->compiled->getVariableNames();

//       // Combine parameter names with extracted values
//       foreach ($keys as $index => $key) {
//           $parameters[$key] = isset($values[$index]) ? $this->prepareParameterValue($values[$index]) : null;
//       }

//       return $parameters;
//   }

// }
